CREATE TABLE `tb_item` (
  `id` bigint(10) NOT NULL AUTO_INCREMENT COMMENT '商品id，同时也是商品编号',   配置到id唯一域中
  `title` varchar(100) NOT NULL COMMENT '商品标题',
  `sell_point` varchar(150) DEFAULT NULL COMMENT '商品卖点',
  `price` bigint(20) NOT NULL COMMENT '商品价格，单位为：分',
 
  `image` varchar(500) DEFAULT NULL COMMENT '商品图片',
  `cid` bigint(10) NOT NULL COMMENT '所属类目，叶子类目',
  
  `status` tinyint(4) NOT NULL DEFAULT '1' COMMENT '商品状态，1-正常，2-下架，3-删除',
  
  item_title:便宜的手机 AND item_status:1
  
  是否分词      分词是为了索引      索引不一定要分词
  是否索引      索引是为了查询      为什么要查询：需求决定      权利最大的产品经理
  是否存储      存储是为了展示

<field name="item_title" type="text_ik" indexed="true" stored="true" /> 
<field name="item_sell_point" type="string" indexed="false" stored="true" />
<field name="item_price" type="long" indexed="true" stored="true" />
<field name="item_image" type="string" indexed="true" stored="true" />
<field name="item_cid" type="long" indexed="true" stored="true" />
<field name="item_status" type="int" indexed="true" stored="true" />
  
  
  
  
  
  